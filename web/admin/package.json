{
  "name": "@nailzify/admin-page",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Embedded Shopify admin page: upload PDFs, watch them get indexed.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "vite": "^6.0.0"
  }
}
